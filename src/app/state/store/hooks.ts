/*
import {
  useStoreActions as useStore,
  useStoreDispatch as useDispatch,
  useStoreState as useState
} from 'easy-peasy';
import { ApplicationStoreModel } from 'app/state/models';

export const useStoreActions = useStore<ApplicationStoreModel>();
export const useStoreDispatch = useDispatch<ApplicationStoreModel>();
export const useStoreState = useState<ApplicationStoreModel>();
*/
